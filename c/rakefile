
COMPONENTS = %w(
    Crud
    DynamicType
    FileBrowser
    HelloWorld
    Notifications
    ReplicatorRequest
    Select
    Serializer
    SimpleMeteor
    Subscriber
    SysMonitor
    Mqtt
)

require "#{ENV['CORTO_BUILD']}/forward"
