#package ::Replicator

class CarBrands: replicator, hidden::
    alias mount: replicator::mount
    items: list{result}, private
    int16 construct()
    resultIter onRequest(object parent, string expr)
