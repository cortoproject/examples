#package ::CarBrands

class Connector: replicator, hidden::
    alias mount: replicator::mount
    items: resultList, private
    int16 construct()
    resultIter onRequest(string parent, string expr, bool setContent)
